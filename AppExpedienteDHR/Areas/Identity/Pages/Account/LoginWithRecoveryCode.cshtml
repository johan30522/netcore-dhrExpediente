@page
@model LoginWithRecoveryCodeModel
@{
    ViewData["Title"] = "Verificación de código de recuperación";
}

<h1>@ViewData["Title"]</h1>
<hr />
<p>
    Has solicitado iniciar sesión con un código de recuperación. Este inicio de sesión no será recordado hasta que proporciones
    un código de la aplicación autenticadora al iniciar sesión o desactives la autenticación de dos factores (2FA) e inicies sesión nuevamente.
</p>
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
            <div class="form-floating mb-3">
                <input asp-for="Input.RecoveryCode" class="form-control" autocomplete="off" placeholder="Código de recuperación" />
                <label asp-for="Input.RecoveryCode" class="form-label"></label>
                <span asp-validation-for="Input.RecoveryCode" class="text-danger"></span>
            </div>
            <button type="submit" class="w-100 btn btn-lg btn-primary">Iniciar sesión</button>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}